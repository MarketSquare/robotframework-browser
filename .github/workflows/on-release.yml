name: Release tasks

on:
  release:
    types: [ published ]


docker-image:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Invoke build tool
        run: |
        python -m pip install --upgrade pip
        pip install invoke
      - name: Build and publish image
        inv docker-publish

