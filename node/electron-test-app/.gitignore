# Runtime IPC socket created by Electron when nodeIntegration is enabled
PIPE

# Lock file generated by npm install – not committed so the CI always resolves
# the latest compatible versions rather than pinning to a snapshot.
package-lock.json
